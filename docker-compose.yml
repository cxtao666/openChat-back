version: '3'
services:
  meeting-chat:
    image: meeting-chat  #镜像
    container_name: meeting-chat  #容器名称  
    build:
      context: .
      dockerfile: ./build/meeting-chat.Dockerfile 
    ports:
      - 127.0.0.1:5001:5001
